type ChallengeCertificate @table @sealed {
    domain: ID! @primaryKey
    issueDate: Date
    renewalDate: Date @indexed
    challengeToken: String @indexed
    challengeContent: String
    inProgress: Boolean
}